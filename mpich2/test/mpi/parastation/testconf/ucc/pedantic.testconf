# Enable the use of UCC (if available) and turn on the reporting of warnings
# (Fallbacks caused by a lack of UCC support are now reported.)
SCRIPTDIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
source ${SCRIPTDIR}/../ucc.testconf
export PSP_UCC_VERBOSE=2
export MPIR_CVAR_CH4_UCC_VERBOSITY_LEVEL=2
export UCC_LOG_LEVEL=WARN
# Since psmgmt does not automatically export all variables, we have to do so explicitly in such cases:
mpiexec --exports= -np 1 true 2> /dev/null && export MPITEST_MPIEXECARG="${MPITEST_MPIEXECARG} --exports=MPIR_CVAR_CH4_UCC_VERBOSITY_LEVEL"
